// from misc.proto

syntax = "proto3";

message Vec3 {
    float x = 1;
    float y = 2;
    float z = 3;
}

/*
message Clearance {
    float axial = 1;
    float radial = 2;
}

message Box {
    Vec3 min = 1;
    Vec3 max = 2;
}

message Response {
    oneof type {
        Errors error = 1;
        ToolSetRequiredForAnalysis tool_set_required_for_analysis = 2;
    }
}

message Errors {
    string error_msg = 1; // empty if no errors
}

message ToolSetRequiredForAnalysis {
}
*/